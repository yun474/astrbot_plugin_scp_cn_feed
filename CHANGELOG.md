# 更新日志

## 0.4.0 - 2026-08-03

- 命令组由 `/scpfeed` 改为更简短的 `/scp`。
- `/scp 订阅` 移除来源参数，执行后默认订阅精品 SCP、精品原创故事和竞赛与活动三个来源。
- 取消命令改为 `/scp 取消订阅`，同样移除来源参数并默认取消全部来源。

## 0.3.0 - 2026-07-17

- 新增可在 AstrBot 配置页直接增删改的订阅会话列表。
- `/scpfeed 订阅` 和 `/scpfeed 取消` 会自动把会话订阅同步回插件配置；旧版 `state.json` 订阅会在首次升级时自动迁移。
- 新增更新推送形式三选一：整份日报、新增模块截图、纯文字。
- 自动轮询和 `/scpfeed 检查` 统一遵循更新推送形式；图片发送失败时自动回退文字。
- 日报加入 SCP 基金会 Logo，作者、评分等元信息改为蓝色，正文超链接继续使用红色。
- README 加入使用中文站实时数据生成的日报预览图。
- 不同来源的抓取错误改为相互隔离，单个来源失败不会再中断整轮自动推送。
- 新增订阅同步、三种推送形式、抓取容错与日报样式测试。

## 0.2.0 - 2026-06-04

- `/scpfeed 日报` 改为使用本地 Playwright 渲染 HTML 图片卡片。
- 自动推送和手动检查新增内容时，支持截图 SCP 中文站真实网页区域。
- 新增 `/scpfeed 截图 <来源>`，可主动截取单个来源当前内容的网页区域。
- 精品 SCP / 精品原创故事的推送截图和主动截图消息会附带原文链接。
- 优化竞赛信息解析，竞赛名和正文中的链接文字会正确显示并高亮。
- 新增渲染相关配置，并在截图失败时自动回退为文本消息。
